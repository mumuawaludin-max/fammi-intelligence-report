# Google Apps Script sudah tidak dipakai sejak migrasi ke Supabase + RLS (2026-07).

# Opsional. Kalau tidak diisi, web/src/lib/supabase.js jatuh ke nilai proyek FIR yang sudah
# di-hardcode di sana. Isi berdua kalau mau menunjuk proyek Supabase lain (mis. staging),
# atau kalau mau lingkungan lokal tidak bergantung pada nilai hardcoded. Nilainya anon key
# publik, aman dipublikasikan; RLS di Postgres yang menjaga baris mana yang boleh dibaca.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
